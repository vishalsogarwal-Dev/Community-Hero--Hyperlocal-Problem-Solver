# Comma-separated Gemini API keys. The agent tries them in order and
# rotates to the next one if a call fails or returns empty — same
# behaviour as GEMINI_API_KEYS in the old web-dashboard/src/config/geminiKeys.ts.
GEMINI_API_KEYS=YOUR_GEMINI_KEY_1,YOUR_GEMINI_KEY_2,YOUR_GEMINI_KEY_3
